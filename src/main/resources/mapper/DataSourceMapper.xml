<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sharingplatform.usecase.query.DataSourceMapper">

    <select id="findAll" resultType="com.sharingplatform.usecase.query.ValueResult">
        SELECT
          id,
          text
        FROM
          data_source
    </select>

</mapper>